public class Enums
{
  public enum Side
  {
    top, left, right, back
  }

  public enum MajorCanvas
  {
    menu, inGame, tool, level
  }

  public enum SceneType
  {
     mainMenu, tool, levels, inGame
  }
}